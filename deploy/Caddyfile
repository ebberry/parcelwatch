# Caddy auto-provisions and renews a free Let's Encrypt cert for {$DOMAIN}.
# Requires: DNS A record pointing {$DOMAIN} at the Lightsail static IP, and
# ports 80 + 443 open in the Lightsail firewall.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy web:3000
}
